#include <stdio.h>

/* finding the cube a given number */

int main()
{
	int number; printf("Enter a given number: ");

	scanf("%d", &number);

	printf("The cube of %d is: %d\n", number, number*number*number); return 0;
}
